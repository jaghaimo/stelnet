{
    "name": "prettier-scripts",
    "scripts": {
        "check": "prettier --check src/",
        "format": "prettier --write src/"
    },
    "dependencies": {
        "prettier": "*",
        "prettier-plugin-java": "*"
    }
}
